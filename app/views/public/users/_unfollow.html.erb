<%= form_with(model: current_user.band.active_relationships.find_by(followed_id: @user.band.id),
            html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit 'Unfollow', class: 'btn btn-danger btn-block' %>
<% end %>