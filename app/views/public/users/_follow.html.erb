<%= form_with(model: current_user.band.active_relationships.build, remote: true) do |f| %>
  <%= hidden_field_tag :follow_id, @user.band.id %>
  <%= f.submit 'Follow', class: 'btn btn-primary btn-block' %>
<% end %>