<% unless current_user?(@user) %>
  <div id="follow_form">
    <% if current_user.band.following?(@user.band) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
  </div>
<% end %>